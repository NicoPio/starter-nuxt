# =====================================================
# Database Configuration
# =====================================================
# PostgreSQL connection string for self-hosted Supabase
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# =====================================================
# Better-Auth Configuration (Legacy - to be removed)
# =====================================================
# Secret key for signing tokens (minimum 32 characters)
BETTER_AUTH_SECRET=your-secret-key-min-32-characters-long-change-this-in-production

# Base URL for authentication (used by client)
NUXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3000

# =====================================================
# nuxt-auth-utils Configuration (New)
# =====================================================
# Session encryption password (minimum 32 characters, generate with: openssl rand -base64 32)
NUXT_SESSION_PASSWORD=your-session-password-generated-with-openssl-rand-base64-32

# Feature flag for migration (false | dual | true)
# false: Better Auth only
# dual: Both systems active during migration
# true: nuxt-auth-utils only
USE_NUXT_AUTH_UTILS=dual

# =====================================================
# OAuth Providers (Optional)
# =====================================================
# GitHub OAuth
NUXT_PUBLIC_GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth
NUXT_PUBLIC_GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Apple OAuth
NUXT_PUBLIC_APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# =====================================================
# Stripe Payment Configuration
# =====================================================
# Stripe public key (safe to expose to client)
NUXT_PUBLIC_STRIPE_PUBLIC_KEY=pk_test_...

# Stripe secret key (server-only, never expose to client)
STRIPE_SECRET_KEY=sk_test_...

# Stripe publishable key (server-only for API operations)
STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe webhook secret for signature verification
STRIPE_WEBHOOK_SECRET=whsec_...

# Encryption key for storing Stripe API keys in database (64 hex chars = 32 bytes for AES-256)
# Generate a new key with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Keep this key secure and never commit the real value to version control
STRIPE_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# =====================================================
# Application Configuration
# =====================================================
# Base URL of your application
NUXT_PUBLIC_SITE_URL=http://localhost:3000

# Environment (development, staging, production)
NODE_ENV=development
